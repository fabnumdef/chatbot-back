export enum InboxStatus {
  pending = 'pending',
  to_verify = 'to_verify',
  confirmed = 'confirmed',
  archived = 'archived',
  relevant = 'relevant',
  wrong = 'wrong',
  off_topic = 'off_topic',
}
