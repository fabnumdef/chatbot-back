export enum IntentStatus {
  to_deploy = 'to_deploy',
  active = 'active',
  active_modified = 'active_modified',
  in_training = 'in_training',
  to_archive = 'to_archive',
  archived = 'archived',
}
